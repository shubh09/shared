#include <iostream>#include <vector>#include <cstdio>#include <cstring>#include <string>#include <cmath>#include <algorithm>#include <utility>using namespace std;#define FOR(i,a,b)				for (i=a;i<b;i++)#define s(n)					scanf("%d",&n)#define p(n)					printf("%d\n",n)#define pl(n)					printf("%lld\n",n)#define sd(n)					int n;scanf("%d",&n)#define sl(n)					scanf("%lld",&n)#define sld(n)					long long int n;scanf("%lld",&n)typedef long long ll;class Embassy{ public:       int visaApplication(vector <int> arr,int dl,int ot)  {      int i=0;      int s=arr.size();      int open=arr[0];      int close=open+ot;      int ft=arr[0];      while(i<s)      {        while(i<s&&ft<=close)        {            i++;            if (i!=s) ft+=arr[i];        }        if (i<s)        {            open+=dl;            close=open+ot;            ft=max(open,ft);        }      }      return ft;  }};//for testingint main(){    className cls;    int t=100;    int i;    FOR(i,0,t)    {    }}